<?xml version="1.0" encoding="utf-8"?>
<!--
  The window the OS shows while the process starts, before Flutter draws.
  It is the app's own surface colour rather than white: the default flashed a
  white rectangle in front of a reader opening a dark-themed reading app.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background" />
</layer-list>
